node_modules
.next
.turbo
dist
build
coverage
**/generated
pnpm-lock.yaml
uv.lock
docs/api/openapi.json
*.min.*
# Agent scratch notes — untracked, so CI never sees them. Ignored here too, or
# 20+ false positives bury a real one in `prettier --check .` (which is exactly
# how an unformatted README reached CI).
.superpowers/
.agents/
.claude/
