# @yupay/telegram

Shared Telegram types and small helpers used by the Mini App (and any web pages that need
Telegram Login Widget). **No** validation of `initData` happens here — that is a server
responsibility (see `apps/api/src/yupay/modules/auth/telegram.py`).
