/**
 * Placeholder for the generated OpenAPI types.
 *
 * `pnpm --filter @yupay/api-client gen:api` (called by `make gen-api`) regenerates
 * `src/generated/` and re-exports the real `components` and `paths` types from there.
 * Until the first generation, we ship empty stubs so the workspace type-checks.
 */
export type components = Record<string, never>;
export type paths = Record<string, never>;
