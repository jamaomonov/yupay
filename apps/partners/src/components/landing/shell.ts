/**
 * The one horizontal rhythm every band on the landing shares.
 *
 * The layout is editorial: bands run full-bleed and are separated by hairlines
 * that reach the viewport edge, while their content lines up on a single inner
 * measure. That only reads as deliberate if every band uses the same measure,
 * so it lives here rather than being retyped per component.
 */
export const SHELL = "mx-auto w-full max-w-[1240px] px-5 sm:px-8 lg:px-12";
