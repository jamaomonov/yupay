# Same credentials as postgres.env. The exporter only needs SELECT on
# pg_stat_* and similar internal views — create a dedicated read-only
# role if you want to harden this further.
DATA_SOURCE_NAME=postgresql://yupay_app:CHANGE_ME_to_a_strong_random_string@postgres:5432/yupay?sslmode=disable
