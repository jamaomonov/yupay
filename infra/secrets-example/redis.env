# Redis password (requirepass). Shared by the redis server, redis-exporter,
# and every client URL in api.env (REDIS_URL / DRAMATIQ_BROKER_URL).
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
REDIS_PASSWORD=CHANGE_ME_random_32_chars
