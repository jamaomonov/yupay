# Redis Cache Keys Catalogue

> Every Redis key used by any module **must** appear in this table. New keys without a row
> here are rejected in code review. Keep the table sorted alphabetically by key prefix.

| Key pattern                                                   | Module         | Type             | TTL                                    | Invalidation rule                                                     | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------- | -------------- | ---------------- | -------------------------------------- | --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auth:ipguard:{bucket}:{ip}`                                  | `auth`         | int (counter)    | `auth_ip_guard_window_seconds` (60 s)  | INCR + EXPIRE; resets after each window                               | Per-IP attempt counter for sensitive auth endpoints; `bucket` ∈ `login\|forgot\|check_player`; fails open on Redis error so a cache hiccup never locks out users                                                                                                                                                                                                                                                                                   |
| `auth:pwreset:{jti}`                                          | `auth`         | string           | `jwt_email_token_ttl_seconds` (1800 s) | Consumed on first successful reset (SET NX)                           | Single-use password-reset marker; value `"1"`; SET with NX — if key exists the token is already used (401)                                                                                                                                                                                                                                                                                                                                         |
| `auth:revoked:{jti}`                                          | `auth`         | string           | until JWT expiry                       | On manual revocation                                                  | Access token blocklist                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `auth:tg-widget:{hash}`                                       | `auth`         | string           | remaining widget freshness window      | SET NX, consumed on first use                                         | Single-use Telegram Login Widget marker (replay guard); value `"1"`; `hash` = sha256 of the widget `hash` field                                                                                                                                                                                                                                                                                                                                    |
| `auth:session:{session_id}`                                   | `auth`         | hash             | 30 days                                | On logout / refresh rotation                                          | Refresh token hash, IP, UA                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `catalog:product:{locale}:{id}`                               | `catalog`      | string (JSON)    | 10 min                                 | Invalidated by `catalog.product.updated` event                        | Storefront page data                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `fx:manual:{quote}`                                           | `fx`           | string (JSON)    | none (60 s if the DB row is missing)   | Written on admin `PATCH /admin/fx/rates/{quote}` and on first DB load | `{use_manual, manual_rate, updated_at}`; USD base is implied. `get_rate` reads this before the provider cache (ADR-0055)                                                                                                                                                                                                                                                                                                                           |
| `fx:rate:{base}:{quote}`                                      | `fx`           | string (JSON)    | 15 min                                 | Refreshed by `fx_refresh` periodic job every 5 min                    | Hot read path; falls through to `:stale`. Not written when the quote is on a manual rate                                                                                                                                                                                                                                                                                                                                                           |
| `fx:rate:{base}:{quote}:stale`                                | `fx`           | string (JSON)    | 24 h                                   | Same job that writes the fresh key                                    | Last-known-good for graceful degradation                                                                                                                                                                                                                                                                                                                                                                                                           |
| `idem:{user_id}:{route}:{key}`                                | `core`         | hash             | 24 h                                   | Manual via admin tooling                                              | Mirror of `idempotency_keys` for fast read                                                                                                                                                                                                                                                                                                                                                                                                         |
| `inventory:available:{sku_id}`                                | `inventory`    | int              | 30 s                                   | Invalidated on reservation/issue                                      | Cheap "in stock" check; authoritative read is in DB                                                                                                                                                                                                                                                                                                                                                                                                |
| `orders:status:{order_id}`                                    | `orders`       | string (FSM)     | 1 h                                    | Invalidated on every order event                                      | Planned hot-status read cache — **not yet implemented** (`orders.service` has no Redis calls today); unrelated to the realtime WS gateway added in ADR-0040, which reads Postgres directly and pushes over `realtime:user:{id}` (see below), not this key                                                                                                                                                                                          |
| `playercheck:g2b:{game_code}:{server_id\|-}:{player_id_hash}` | `integrations` | string (JSON)    | 300 s                                  | TTL only                                                              | Advisory storefront nickname lookup (ADR-0031); `player_id_hash` is `_hash_short(player_id)` (sha256, 12 hex chars) — never the raw id, per §9; best-effort cache — Redis errors fall through to a live G2B call                                                                                                                                                                                                                                   |
| `playercheck:waxpeer:{login_hash}`                            | `integrations` | string (JSON)    | 300 s                                  | TTL only                                                              | Advisory Steam-login check (ADR-0031); stores the three-way `PlayerCheckOut` (`valid`/`invalid`/`error`, `name` always `null`); `login_hash` is `_hash_short(steam_login)` — never the raw login, per §9; best-effort cache — Redis errors fall through to a live Waxpeer call                                                                                                                                                                     |
| `ratelimit:{route}:{ip}`                                      | `core`         | token bucket     | sliding                                | —                                                                     | slowapi                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `realtime:user:{user_id}`                                     | `realtime`     | pubsub (channel) | — (not applicable)                     | — (not applicable)                                                    | **Not a cache key** — a Redis `PUBLISH`/`SUBSCRIBE` fan-out channel for live order-status pushes (ADR-0040). Nothing is ever `GET`, there's no persisted value and no TTL to expire; a message with no subscriber is simply dropped. One channel per user; a WS connection subscribes to exactly one, derived server-side from its verified `kind="ws"` handshake token — never client-supplied — so no connection can read another user's events. |
| `stats:analytics:{business\|ops}:{7d\|30d\|90d}`              | `stats`        | string (JSON)    | 300 s                                  | Time-only (no explicit busting)                                       | Written by the `/analytics` endpoints; trend data, best-effort with live-compute fallback on Redis error                                                                                                                                                                                                                                                                                                                                           |
| `wallet:balance:{user_id}:{currency}`                         | `wallet`       | string (Decimal) | 5 min                                  | Invalidated on any posting                                            | Lazy projection; cold computed via SUM                                                                                                                                                                                                                                                                                                                                                                                                             |

## Conventions

- All keys are colon-separated; **no spaces or trailing slashes**.
- Prefix is **always the owning module's name**.
- Numeric TTLs are explicit in seconds inside the codebase (no implicit "forever" keys).
- Multi-key writes that must succeed atomically use Lua scripts; the script source lives under
  `apps/api/src/yupay/modules/<module>/scripts/`.
