# Blog — M3 leftover: FAQ editor, event/pin/schedule

TipTap and `blog_image` already shipped with M1. This pass is the operator
surface the spec still called out: FAQ pairs that match `FAQPage`, the
two-pin cap made visible, a local-time event window, and schedule.

**Spec:** `docs/superpowers/specs/2026-09-12-blog-design.md` §7.4, §5, §15.

## Done

- Per-locale FAQ editor on the post form; save sends `faqs` to the existing
  admin write DTO. Half-filled pairs reject; empty pairs drop.
- Pin checkbox shows how many _other_ published pins the brand already has
  (cap 2). Crossing the cap warns before publish.
- Event dates use local `datetime-local` and preview live / upcoming / ended.
- Drafts can be scheduled; the list shows the due time and a pin mark.
