# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.egg-info/
dist/
build/
.tox/

# --- Node / Next ---
node_modules/
.pnp.*
.yarn/install-state.gz
.next/
out/
.turbo/
.vercel/
.swc/

# --- TypeScript ---
*.tsbuildinfo

# --- Logs ---
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# --- Editors ---
.idea/
.vscode/
!.vscode/extensions.json
!.vscode/settings.recommended.json
*.swp
*.swo

# --- OS ---
.DS_Store
Thumbs.db

# --- Docker / runtime ---
.docker/
docker-compose.override.yml
*.pid

# --- Secrets ---
.env
.env.local
.env.*.local
*.pem
*.key
!*.enc.env
!infra/secrets/*.enc.env

# Live production secrets, mounted by docker-compose.prod.yml (env_file:
# ./secrets/*.env). Present only on the server; never committed. The templates
# in infra/secrets-example/ are the versioned counterpart.
/secrets/

# --- Backups ---
*.dump
*.sql.gz
backups/

# --- Generated ---
docs/api/openapi.json.tmp
packages/api-client/src/generated/
playwright-report/
test-results/

# --- Playwright MCP runtime artefacts ---
# Console logs, YAML page snapshots, and auto-named screenshots produced by
# @playwright/mcp during interactive AI sessions. Local-only — never commit.
.playwright-mcp/
# Ad-hoc screenshots from dev / agent runs that land in the repo root.
/admin-*.png
